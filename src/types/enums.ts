export enum Visibility {
    PUBLIC = "public",
    PRIVATE = "private",
  }

  export enum Priority {
    Low = "low",
    High = "high",
    Critical = "critical"
  }
  
  export enum Status {
    Pending = "pending",
    Completed = "completed",
    InProgress = "in-progress"
  }
  
  export enum Type {
    Bug = "bug",
    Feature = "feature",
    Question ="question",
    Improvement = "improvement",
    Other = "other"
  }